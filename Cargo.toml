[package]
name = "healthmaster"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4", features = ["derive"] }
dotenv = "0.15.0"
serde = { version = "1", features = ["derive"] }
toml = "1.0.6"
